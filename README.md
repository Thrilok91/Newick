# Newick
Changes Child-Parent columns to Newick format

The Python code takes in a Data Frame with 3 columns: Child ID, Parent ID and Incident column with all 1s. The Newick string is stored in 'final_tree' 
